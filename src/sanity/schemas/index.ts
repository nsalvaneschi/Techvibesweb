import { siteSettings } from './siteSettings';
import { solution } from './solution';
import { brand } from './brand';
import { blogPost } from './blogPost';

export const schemaTypes = [siteSettings, solution, brand, blogPost];
